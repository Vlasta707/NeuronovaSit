# Složka pro syrová data (obrázky, popisky) - NEZAHRNUJEME JI, PROTOŽE MÁ BÝT JEN LOKÁLNÍ
# Vyřaďte adresář "vlist_data/" a všechna jeho obsažená data
vlist_data/**
syrova_data/**

# Složka pro zpracovaná data (.npy soubory) - JSOU GENERovaná, NE commitovaná
data/**

# Případné PyTorch modely (pokud je budeš ukládat)
*.pth
*.pt

# Parametry trénování modelu se na síť necommitují, protože jsou specifické pro lokální prostředí a konfiguraci
train_config.json

# Virtuální prostředí
venv/
.venv/
env/
__pycache__/